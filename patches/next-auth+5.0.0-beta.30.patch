diff --git a/node_modules/next-auth/lib/client.js b/node_modules/next-auth/lib/client.js
index 9b8b6c4..7906852 100644
--- a/node_modules/next-auth/lib/client.js
+++ b/node_modules/next-auth/lib/client.js
@@ -16,10 +16,25 @@ export class ClientSessionError extends AuthError {
  * pages *and* in _app.js.
  * @internal
  */
+function credentialsModeForBrowserFetch(url) {
+    if (typeof window === "undefined")
+        return undefined;
+    try {
+        if (!url.startsWith("http"))
+            return undefined;
+        const target = new URL(url).origin;
+        return target === window.location.origin ? undefined : "include";
+    }
+    catch {
+        return undefined;
+    }
+}
 export async function fetchData(path, __NEXTAUTH, logger, req = {}) {
     const url = `${apiBaseUrl(__NEXTAUTH)}/${path}`;
     try {
+        const crossCred = credentialsModeForBrowserFetch(url);
         const options = {
+            ...(crossCred ? { credentials: crossCred } : {}),
             headers: {
                 "Content-Type": "application/json",
                 ...(req?.headers?.cookie ? { cookie: req.headers.cookie } : {}),
diff --git a/node_modules/next-auth/react.js b/node_modules/next-auth/react.js
index fa78e92..984decd 100644
--- a/node_modules/next-auth/react.js
+++ b/node_modules/next-auth/react.js
@@ -150,8 +150,17 @@ export async function signIn(provider, options, authorizationParams) {
     }
     const signInUrl = `${baseUrl}/${providerType === "credentials" ? "callback" : "signin"}/${provider}`;
     const csrfToken = await getCsrfToken();
-    const res = await fetch(`${signInUrl}?${new URLSearchParams(authorizationParams)}`, {
+    const postUrl = `${signInUrl}?${new URLSearchParams(authorizationParams)}`;
+    let signInCredentials;
+    try {
+        signInCredentials = new URL(postUrl).origin !== window.location.origin ? "include" : "same-origin";
+    }
+    catch {
+        signInCredentials = "same-origin";
+    }
+    const res = await fetch(postUrl, {
         method: "post",
+        credentials: signInCredentials,
         headers: {
             "Content-Type": "application/x-www-form-urlencoded",
             "X-Auth-Return-Redirect": "1",
@@ -188,8 +197,17 @@ export async function signOut(options) {
     const { redirect = true, redirectTo = options?.callbackUrl ?? window.location.href, } = options ?? {};
     const baseUrl = apiBaseUrl(__NEXTAUTH);
     const csrfToken = await getCsrfToken();
-    const res = await fetch(`${baseUrl}/signout`, {
+    const signOutUrl = `${baseUrl}/signout`;
+    let signOutCredentials;
+    try {
+        signOutCredentials = new URL(signOutUrl).origin !== window.location.origin ? "include" : "same-origin";
+    }
+    catch {
+        signOutCredentials = "same-origin";
+    }
+    const res = await fetch(signOutUrl, {
         method: "post",
+        credentials: signOutCredentials,
         headers: {
             "Content-Type": "application/x-www-form-urlencoded",
             "X-Auth-Return-Redirect": "1",
