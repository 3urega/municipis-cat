# Obsolet com a únic fitxer: al repo hi ha `keystore.properties` (ruta + alias).
# Els secrets van a `keystore.local.properties` (vegeu `keystore.local.properties.example`) o a ANDROID_KEYSTORE_PASSWORD.

storeFile=upload-key.jks
keyAlias=upload
