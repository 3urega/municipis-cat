# Copia aquest fitxer a:  keystore.local.properties  (no es versiona; està al .gitignore)
#
# Gradle usa aquestes contrasenyes per signar el AAB de release (upload-key.jks).
# Alternativa: no usar aquest fitxer i definir a PowerShell abans de gradlew:
#   $env:ANDROID_KEYSTORE_PASSWORD = "..."
#   $env:ANDROID_KEY_PASSWORD = "..."   # només si la key té altra clau que el keystore
#
# storePassword = contrasenya del FITXER .jks (la que vas posar en crear el keystore amb keytool)
# keyPassword   = sovint la mateixa; només diferent si keytool va demanar contrasenya separada per a la clau

storePassword=Gutxeisud2_
keyPassword=Gutxeisud2_